import styles from 'components/footer/Footer.module.css';
import { ComponentReturnType } from 'types';

export const Footer = (): ComponentReturnType => (
  <div className={styles.footer}>footer</div>
);
